# RT-Thread building script for component

from building import *

cwd = GetCurrentDir()
src = Glob('*.c')
CPPPATH = [cwd]

group = DefineGroup('fs/pipefs', src, depend = ['RT_USING_DFS', 'RT_USING_DFS_PIPEFS'], CPPPATH = CPPPATH)

Return('group')
